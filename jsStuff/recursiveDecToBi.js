const theNumber = 1234;

const convertToBinary = (theNumber) => {
    let binaryRepresentation = '';
    moduloPrepender(theNumber, binaryRepresentation)
    console.log(binaryRepresentation)
    return theArray
}

const moduloPrepender = (theNumber, theArray) => {
    return moduloPrepender(parseInt(theNumber / 2)) + theArray;
};

convertToBinary(theNumber);
